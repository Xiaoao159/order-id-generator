{
  "name": "orderid-generator",
  "version": "1.0.0",
  "description": "Generatore di un id per gli ordini",
  "main": "dist/index.js",
  "scripts": {
    "del": "rimraf dist",
    "build": "yarn del && NODE_ENV=production babel index.js --out-dir dist",
    "start": "yarn build && node dist"
  },
  "author": "Saro Vindigni",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-eslint": "^7.2.3",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-3": "^6.24.1",
    "eslint": "^4.1.1",
    "rimraf": "^2.6.1"
  }
}
